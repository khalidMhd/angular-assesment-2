import { InCartDirective } from './in-cart.directive';

describe('InCartDirective', () => {
  it('should create an instance', () => {
    const directive = new InCartDirective();
    expect(directive).toBeTruthy();
  });
});
